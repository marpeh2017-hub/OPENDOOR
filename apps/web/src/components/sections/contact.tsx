// This section is unused in the new layout — CtaSection handles the contact form.
// Keeping file to avoid import errors in case other code references it.

export function ContactSection() {
  return null
}
