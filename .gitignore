# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
.next/
out/
build/

# Turbo
.turbo/

# Environment
.env
.env.local
.env.*.local
!.env.example

# Database
*.db
*.sqlite

# Logs
*.log
logs/
npm-debug.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Test coverage
coverage/
.nyc_output/

# Prisma
prisma/migrations/dev/

# AWS
.aws/

# Terraform
.terraform/
*.tfstate
*.tfstate.backup
*.tfvars
!*.tfvars.example

# Build caches
*.tsbuildinfo

# Redis persistence snapshot. NEVER commit: it contains live runtime state —
# `jwt:revoked:*` session identifiers and `otp:resend:*` counters.
dump.rdb

# Database dumps. A dump contains every resident's personal data in plaintext,
# including encrypted-at-rest columns in their encrypted form plus everything
# that is not encrypted. It must never reach the repository.
backups/
*.dump
*.sql.gz
